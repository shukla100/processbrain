Leadership sync — scratch notes

- Growth wants faster time-to-first-deposit. Biggest drag right now seems to be
  the compliance review step for flagged applications — no SLA on it at all.
- Question came up: who actually owns the client relationship between "account
  approved" and "Jordan's team gets pinged"? Seems like a gap.
- Treasury cash management setup (sweep accounts, idle cash yield options) isn't
  a formal process yet — clients ask about it but there's no defined owner or
  workflow.
- Action item: get the account onboarding flow documented end to end so we can
  see where the actual delays are instead of guessing.
